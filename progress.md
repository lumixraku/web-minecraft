# Progress

## 2026-06-12 水面菲涅尔倒影（branch: water-fresnel-reflection）

### 目标

水面原来只用一个纯色（`uSkyCol`）模拟菲涅尔反光，没有真正的倒影。目标是让岸边方块、天空、太阳和月亮都能按菲涅尔规律反射到水面上：视角越平反射越强，俯视时基本只看到水色。

### 实现：平面反射（planar reflection）

全世界的水面共用同一个高度（`WATER_Y = SEA_LEVEL + 0.875`），所以每帧只需要一次镜像渲染，所有区块的水面共享一张反射纹理。

- **镜像相机**（`water.js` 的 `renderReflection`）：把主相机的位置、朝向、up 向量关于 `y = WATER_Y` 平面镜像，得到虚拟相机，每帧在主渲染之前把场景渲染到一张 1/4 分辨率的 render target（涟漪扰动 + 柔和画风下看不出分辨率差异，比半分辨率省约一半开销）。
- **斜裁剪近平面**（Lengyel oblique clipping）：直接改虚拟相机的投影矩阵，把水面以下的几何体裁掉，防止水下地形"漏"进倒影。对任何材质都有效，不依赖 three.js 的 clipping planes。
- **shader 采样**：顶点着色器算投影纹理坐标（`uReflMatrix * 世界坐标`），片元着色器用水面法线扰动 uv（`+ nrm.xz * 0.35`）做涟漪，再按原有菲涅尔系数 `pow(1 - V·N, 3)` 混入水色。
- **细节**：反射渲染时隐藏水面材质自身、复用当帧阴影贴图；相机在水下时关闭反射（`uReflStrength = 0`）回退到纯天空色；world dispose 时释放 render target。

### 问题一：太阳/月亮没有倒影，只有方块有

原因有两个，叠加在一起：

1. **HDR 被截断**：太阳不是场景物体，是天空穹顶 shader 里的 HDR 亮斑（亮度 ≈ 2.6）。反射纹理默认是 8-bit，亮度被截到 1.0，和日落时本来就接近 1.0 的天空糊在一起，没有对比度。
2. **菲涅尔压制**：太阳升高后，看它倒影的视线变陡，菲涅尔系数趋近 0（约 0.05 下限），残余亮度 0.05 × 1.0 完全不可见。方块亮度低、又靠掠射角反射，所以只有方块看得到。

修复：

- 反射 render target 改用 `HalfFloatType`，保住 HDR 亮度，倒影里的太阳能触发 bloom；
- 在水面 shader 里加一个**解析的镜面光盘**：`reflect(-V, nrm)` 和当前光源方向（太阳或月亮）点积过 `smoothstep` 出一个清晰的圆盘，不依赖纹理精度，任何角度都存在，权重 `(0.3 + 1.2 * fres)`。

### 问题二：贴近水面看日落，太阳倒影只显示一部分

相机贴近水面、太阳低悬（仰角 2~4°）时，倒影里的太阳缺了下半部分。

原因：斜裁剪里设了 `CLIP_BIAS = 0.05`。这个偏移在**裁剪空间**生效，换算回世界空间时随深度放大——在天空穹顶的距离（700 单位）上，等效于把水平面以上约 35 个单位（≈2.9°）的天空也裁掉了。低悬太阳恰好就在这个角度范围内，倒影被齐腰裁断。

修复：`CLIP_BIAS` 改为 0。反射渲染时水面本身是隐藏的，不存在 z-fighting，本来就不需要偏移。改完后贴近水面看日落，能看到完整的太阳倒影和光柱。

### 其他

- 昼夜流速改为与现实 1:1（`DAY_LENGTH = 86400`）。注意：改 `config.js` 后浏览器可能缓存旧文件，验证要强制刷新。`T` 键仍是 40 倍速快进。
- 性能：反射是每帧多渲染一次场景，1/4 分辨率下实测开销可忽略（测试环境 34fps vs 无反射 27fps，误差内）。

### Commits

| commit | 内容 |
|--------|------|
| `9400b93` | 水面平面反射 + 菲涅尔混合 |
| `e59fa48` | 太阳/月亮倒影（HDR 纹理 + 解析光盘 + 去掉裁剪偏移） |
| `80a320b` | 昼夜周期真实时间 |
